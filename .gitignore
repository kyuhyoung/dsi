# 참고 자료 (소스 아님, 큰 파일)
/AI 활용 공모전*.pdf
/image*.png

# Claude Code 로컬 사용자 설정
.claude/settings.local.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
install.log

# 생성된 산출물 (재현 가능, 추적 불필요) — *내부 파일* 단위 ignore (디렉토리는 추적)
output/**
# 단 *시스템 자산*은 화이트리스트로 추적 (집/회사 환경 이전 시 필요)
!output/**/
!output/learned_patterns_*.yaml
!output/proposal_style_*.md
!output/**/rfp_analysis_*.yaml
!output/**/rfp_analysis_*.report.md
!output/**/사전질의_*.md
!output/D_*_filled_cells.yaml
!output/comparison_D_*.md

# Node
/node_modules/

# 빌드/실행 캐시
/.cache/

# LibreOffice/Office 임시 잠금 파일
.~lock.*#

# Template 백업·캐시·생성물 (재현 가능)
templates/*.style.yaml.bak
templates/*.style.yaml.bak.*
templates/*.style.yaml.auto
templates/*.style.yaml.manual
templates/*.pptx.old
templates/.sync.log
templates/*.thumbnails/

# 큰 template .pptx (GitHub 100MB 제한 회피 — 별도 공유)
# [다비오]의 대괄호는 glob 문자클래스라 와일드카드로 매칭
templates/*회사소개자료*.pptx
templates/*회사소개자료*.raw.yaml
templates/*회사소개자료*.thumbnails/

# Samples (큰 파일·임시)
samples/proposals/
