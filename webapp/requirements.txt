claude-agent-sdk>=0.2.91
streamlit>=1.58.0
pyyaml>=6.0
PyMuPDF>=1.24          # PDF page check (optional)
# Auth = Claude Code CLI (Max subscription) -- no separate API key needed.
# Build steps (.hwp<->.hwpx convert, PDF) need Hancom Office COM (Windows) -- not a pip target.
# scripts/ deps such as pywin32, lxml must already be installed in the project root env.
# NOTE: keep this file ASCII-only. pip reads requirements.txt with the system encoding
#       (cp949 on Korean Windows); non-ASCII comments raise UnicodeDecodeError on -r install.
