claude-agent-sdk>=0.2.91
streamlit>=1.58.0
pyyaml>=6.0
PyMuPDF>=1.24          # PDF 페이지 확인(선택)
# 인증은 Claude Code CLI(Max 구독) — 별도 API 키 불필요.
# 빌드 단계(.hwp↔.hwpx 변환·PDF)는 한컴 오피스 COM(Windows) 필요 — pip 대상 아님.
# 기존 scripts/ 가 의존하는 pywin32·lxml 등은 프로젝트 루트 환경에 이미 설치되어 있어야 함.
